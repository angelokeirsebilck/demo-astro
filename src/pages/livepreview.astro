---
import DefaultLayout from "@layouts/DefaultLayout.astro";
import { useGraphqlQuery } from "@composables/useGraphqlQuery";
import { entryQuery } from "@graphql/entry.gql";
import { GetSiteIdFromLang } from "@composables/useGetSiteIdFromLang";

import FetchPreviewData from "@components/FetchPreviewData.svelte";
---

<script>
  import Test from "@components/Test.astro";

  const title = "coming frm preview";

  let EntryTypeElement: any = title;
  if (title) {
    EntryTypeElement = Test;
  }
</script>

<h1>{title}</h1>
<!-- <DefaultLayout title={entry?.title} groupHandle={entry.?groupHandle} id={entry.?id} siteId={entry.?siteId} sectionHandle={entry.?sectionHandle} typeHandle={entry.?typeHandle} lang={entry.?lang}  uri={entry.?uri} slug={entry.?slug} /> -->
